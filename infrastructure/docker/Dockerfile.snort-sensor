FROM python:3.11-slim

ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

WORKDIR /sensor
COPY infrastructure/docker/snort_sensor_proxy.py /sensor/snort_sensor_proxy.py

EXPOSE 8080
CMD ["python3", "/sensor/snort_sensor_proxy.py"]
